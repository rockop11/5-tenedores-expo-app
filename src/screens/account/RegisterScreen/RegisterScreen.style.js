import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    image: {
        resizeMode: 'contain',
        height: 150,
        width: "100%",
        marginTop: 50,
    },
    content: {
        marginHorizontal: 40,
    }
})